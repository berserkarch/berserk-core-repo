GRUB_DISTRIBUTOR="BerserkArch"
GRUB_DISABLE_SUBMENU=y

if [ -f /boot/vmlinuz-berserk-kernel ]; then
    GRUB_TOP_LEVEL="/boot/vmlinuz-berserk-kernel"
    GRUB_DEFAULT="gnulinux-berserk-kernel-advanced-$(findmnt -n -o UUID /)"
else
    GRUB_DEFAULT=0
fi
